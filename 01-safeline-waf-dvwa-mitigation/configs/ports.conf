# Configuration extracted from /etc/apache2/ports.conf
# Apache listening port modified to 8080 to allow SafeLine WAF proxy on port 80

Listen 8080

<IfModule ssl_module>
    Listen 443
</IfModule>

<IfModule mod_gnutls.c>
    Listen 443
</IfModule>